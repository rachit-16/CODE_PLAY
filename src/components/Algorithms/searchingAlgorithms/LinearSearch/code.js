const code = `
void binearSearch(int ar[], int n, int ele) {
    int i;
    int index = -1;

    for(i=0 ; i<n ; i++) {
        if(ar[i] == ele) {
            index=i;
            break;
        }
    }

    if(idx == -1) {
        cout<<"Element Not Present";
    }
    else {
        cout<<"Element Present"
    }
}
`;

export default code;
